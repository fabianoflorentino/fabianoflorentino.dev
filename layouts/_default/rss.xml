{{- $pctx := . -}}
{{- if .IsHome -}}{{ $pctx = .Site }}{{- end -}}
{{- $pages := slice -}}
{{- if or $.IsHome $.IsSection -}}
{{- $pages = $pctx.RegularPages -}}
{{- else -}}
{{- $pages = $pctx.Pages -}}
{{- end -}}
{{- $limit := .Site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
{{- $pages = $pages | first $limit -}}
{{- end -}}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>{{ .Site.Title }}</title>
    <link>{{ .Permalink }}</link>
    <description>{{ .Site.Params.description | default .Site.Title }}</description>
    {{- with .Site.Params.about.image }}
    <image>
      <title>{{ $.Site.Title }}</title>
      <url>{{ . | absURL }}</url>
      <link>{{ $.Site.BaseURL }}</link>
    </image>
    {{- end }}
    {{- with .Site.Language.LanguageCode }}
    <language>{{ . }}</language>
    {{- end }}
    {{- if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" }}</lastBuildDate>
    {{- end }}
    {{- with .OutputFormats.Get "RSS" }}
    {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{- end }}
    {{- range $pages }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" }}</pubDate>
      {{- with .Site.Language.LanguageCode }}
      <language>{{ . }}</language>
      {{- end }}
      <guid>{{ .Permalink }}</guid>
      {{- with .Params.description }}
      <description>{{ . | html }}</description>
      {{- else }}
      <description>{{ .Summary | html }}</description>
      {{- end }}
      {{- with .Params.image }}
      {{- $enclosure := . | absURL }}
      {{- with resources.Get (strings.TrimPrefix "/" .) }}{{ $enclosure = (.Resize "1200x").Permalink }}{{ end }}
      <enclosure url="{{ $enclosure }}" type="image/jpeg" length="0"></enclosure>
      {{- end }}
      {{- with .Params.tags }}
        {{- range . }}
      <category>{{ . }}</category>
        {{- end }}
      {{- end }}
      <content:encoded>{{ "<![CDATA[" | safeHTML }}{{ .Content | safeHTML }}{{ "]]>" | safeHTML }}</content:encoded>
    </item>
    {{- end }}
  </channel>
</rss>
